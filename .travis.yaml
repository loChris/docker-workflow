language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t lochris/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true lochris/docker-react npm run test -- --coverage
